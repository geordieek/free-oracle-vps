- name: Install tools
  hosts: all
  vars_files:
    - ../vars/main.yml
  become: true
  tasks:
    - name: Update and upgrade apt packages
      apt:
        update_cache: yes
        upgrade: dist

    - name: Install basic tools
      apt:
        name:
          - curl
          - vim
          - git
        state: present
